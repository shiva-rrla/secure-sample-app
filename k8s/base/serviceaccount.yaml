apiVersion: v1
kind: ServiceAccount
metadata:
  name: secure-sample-app
  namespace: secure-sample-app
  annotations:
    # If using AWS IRSA, add the IAM role ARN here
    # eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/SecureSampleAppRole
automountServiceAccountToken: false
